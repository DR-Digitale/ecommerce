/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    domains: ['lepetitmondedeslibellules.fr'],
  },
}

module.exports = nextConfig
